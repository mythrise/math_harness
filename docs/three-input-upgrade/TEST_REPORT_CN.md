# v0.5.0-rc1 三模式升级实测报告

日期：2026-09-09。源码基线：`mythrise/math_harness @ 8d9c59619edeb0928e7e24688e074fe1cff91250`。

## 结论与发布边界

本包包含36个生产源码/配置/测试/文档路径：10个现有文件的完整替换代码、26个新增文件。10个旧文件的Git blob均与本次读取的当前GitHub提交相符；每个新文件的SHA-256写入UPGRADE_MANIFEST.json。生产代码没有替换原MOSAIC、OurWork、Exa传输、审查板、实验执行器或TeX隔离器。

本次验证了输入、候选思路接入、论文修订及对应确定性合同，并在组件测试环境中完成idea与scratch到PDF/支撑ZIP的完整控制器路径。**没有完成修改后整个最新GitHub checkout的全量回归；没有真实Codex/Claude调用、用户Exa HTTP或Docker执行。** R2真实/完整组件联合运行、正式比赛人工签核、真实题目质量均为NOT_RUN。这是候选发行版，不声明稳定生产验收或获奖能力。

### 本地运行镜像与生产代码必须区分

容器不能克隆完整仓库。测试使用已上传源码、支撑包与读取的当前模块重建本地测试环境。

- 修改的10个原文件均先匹配Git blob，完整替换源码是这次交付的真实修改。
- contracts.py和research.py已恢复为当前GitHub精确版本并复核blob，分别为`da8ba54a14a72b03ccc46c45fd17baaf9d073717`、`66c75d216c65fbe204512088bf42a5743f8fbaf1`，但本包不替换它们。
- 本地未修改依赖中，审查板、文献旧适配器、存储/执行模块及部分图表/算法模块包含早期版本或最小兼容恢复；本地PaperKit样式与几何检查器属于明确标记的组件测试重建，不是生产隔离编译器的验收。
- **这些测试镜像文件没有放进payload**，不会覆盖用户仓库的新修复。本包也没有把本地镜像打成“完整最新版”。
- 原仓库CI产物保留为UPSTREAM_REFERENCE_ONLY，仅供比较；其中任何测试成绩都不计入本次成绩。

## 自动测试

`evidence/pytest-components.xml`与日志记录：**248 passed**。

| 范围 | 通过数 |
|---|---:|
| 新文档导入/保护/回写 | 42 |
| 新输入模式、来源与冻结 | 36 |
| 新idea提取/反方/采纳映射 | 29 |
| 新修订流程/拒绝/回放 | 14 |
| 新CLI/角色/技能/Controller接线 | 24 |
| 既有materials局部回归 | 103 |
| 合计 | 248 |

其中新增测试为**145项**。既有部分会随新增角色动态参数化，不能把103描述为固定不变的旧版测试数。另有安装器/完整源码导出器20项，单独计数。

交付ZIP第一次封装后，实际解压、核对清单、扫描嵌套压缩包无字体文件，并将payload应用到全新的同范围组件镜像：再次**248项通过**（19.45秒）。这仍是组件镜像验收，不是最新完整checkout的回归；日志见pytest-extracted-components.*。随后只追加验收文档与证据，未改生产payload。

覆盖的关键负例包括：原题与idea混放、输入摘要改变、旧工作区强行加sidecar、模式参数冲突、假网页成绩进入候选事实、未覆盖来源块、未对应实际小问、采纳假设未进入正式假设表、无文献通道启动真实idea运行、数字/单位/公式/引用被改、意义强度词被删、脚本与TeX输入被执行、未知或重复段落补丁、DOCX压缩炸弹/路径穿越/实体DOCTYPE、冻结产物被改、供应商失败及未知执行状态。

## 三路组件执行

以下模型响应均为预写的固定回复；模型质量不是测试目标。审查里的Claude故障是注入，不是实际Claude服务故障。检索走旧适配器的固定来源重放，不是Exa R2或真实HTTP。数值与XeLaTeX确实执行。

| 模式 | 成功固定回复 | 调用预算预占（含故障检查） | 实际数值作业 | 最终状态 |
|---|---:|---:|---:|---|
| idea | 69 | 71 | 22：开发12、确认10 | DEMO_COMPLETE_NOT_LIVE_VALIDATED |
| scratch | 56 | 58 | 22：开发12、确认10 | DEMO_COMPLETE_NOT_LIVE_VALIDATED |
| revise | 5 | 7 | 0 | REVISION_FIXTURE_COMPLETE_NOT_LIVE_VALIDATED |

三个工作区恢复时均把任何新增模型/检索/执行调用设置为立即报错：全部成功回放，**0新增回复、0新增作业、封存产物摘要不变**。详细identity在各模式的three-input-replay.json中。

idea固定输入包含可用MOSAIC建议、待检验假设、声称“提高99%”的数字及“忽略审查”的指令。程序取得逐项处理记录：两项MODIFY、两项REJECT；采纳假设有正式假设索引，网页99%没有成为测量claim。这个结果验证路径和合同；不能推出真实语言模型在所有模糊输入上都会同样判断。

## 实际论文与支撑包

idea与scratch均真实编译两遍。最终idea PDF125页：摘要第1页、正文/声明/参考文献共3页、附录第5页开始。绝大部分页数是完整源码；不是正式题目应有的篇幅目标。组件检查PASS，没有缺字、未解析引文或Overfull hbox/vbox。生产版完整PaperKit/Docker验收仍NOT_RUN。

视觉查看覆盖摘要与全部正文、附录首尾样本，以及AI使用详情首尾样本；未逐页人工阅读整部源码附录。最终正文与AI详情图像样本见evidence/visual。

从最终idea支撑ZIP解压到独立目录，使用已知固定演示代码分别复现baseline和c0、确认种子701。答案JSON与独立评测JSON都与原作业**逐字节一致**，四项比对及摘要见reproduction_summary.json。这不是未经许可执行任意模型代码。

## 真实DOCX回写与渲染

使用一份自建DOCX，含普通中文段落、真实OMML公式与表格。应用一次经过固定审查回复批准的语言修订后：

- 原文件摘要不变。
- 公式与表格XML保持一致。
- 除document.xml外16个ZIP成员逐字节一致。
- revised.docx实际渲染，1页，已视觉查看，无丢失公式、表格或明显排版问题。

这是实际DOCX操作/渲染测试，但语义审查仍是固定回复。不能据此声明任意带批注、脚注、浮动文本框或复杂域的DOCX都完整受支持；不支持区域会保护/报告，不假装改过。

论文修订不会重新跑原实验。PDF-only输入只输出带限制说明的Markdown；TeX控制段不执行。已有论文的原AI历史不会被新编辑回执替代。

## 真实失败与处理

首次局部回归245通过、2失败，原因是本地重建误用了旧contracts，不支持已有上游的answer_type；恢复当前精确版本后重新测试。另一次全链在旧research镜像的共享输入初次发布上出现竞态；当前上游本来已在启动workers前冻结该项，本地恢复当前精确版本后重跑。**这两项不是本次新发现并修复的最新仓库缺陷。** 原日志保留并明确分类。

实际AI详情编译发现新增状态字符串过长导致Overfull hbox，已在materials_paper.py中为转义后的下划线加入合法换行点；最终重新运行idea/scratch流程与回放。DOCX导入则补充了UTF-16 DOCTYPE拒绝测试，避免只检查UTF-8字节模式。

## 生产环境待验收

在真实完整checkout中应用并重建原执行/TeX镜像后，必须跑全仓pytest、三路默认Docker演示、idea/scratch的`--r2`及replay、实际CLI schema与公开Exa烟测。再选择授权历史题检验模型选择、引文、假设检验及最终论文质量。远程GitHub Actions工作流已提供但未推送执行；正式竞赛需要真实人工主导与逐项核验，不能把这些组件通过标记当成人工签核。
