# 正式比赛只保留本地材料

操作员在 2026-09-10 明确要求：停止并清理定日镜场测试，完成当前 harness 源码推送，之后真正赛题生成的论文绝不能再推送。

本次交付将修复后的完整 harness 源码及不含真实题目正文的工程验收说明推送至 GitHub。定日镜场专用输入、工作区和原始测试记录已按要求清理；用户停止不是完整求解成功。

正式比赛工作区固定放在 `/Users/mythrise/CUMCM_2026_PRIVATE`，它位于所有 Git 检出之外。原题、附件、外部建议、模型调用、实验结果、论文和支撑包均保留于该目录，不复制进源码库，不上传至 GitHub、PR、issue、CI artifact 或公开链接。现有 `.dockerignore` 继续只允许必要源码进入镜像构建上下文。

本次最终源码推送之后，通过仓库本地 `core.hooksPath` 指向 `.githooks`，启用拒绝所有后续推送的 `pre-push` 钩子。该设置覆盖同一仓库的现有 worktree。不能为上传比赛内容关闭或绕过它。新克隆不会继承本地 Git 配置，开始使用前也须按本文件和 AGENTS.md 设置推送锁。

之后如操作员另行明确要求推送纯 harness 修复，必须先审核本次完整提交差异，只包含框架源码与公开合成测试；该授权始终不包含真实赛题或论文。不要把比赛目录强制加入版本控制，也不要为上传材料创建另一仓库或分支。
