# rc3 声明修复本机验收（2026-09-10）

代码检查点：`fb66eac5bd0e2146813b78cb372d24f453779386`。本地与 GitHub 工程验收均通过。之后仅增加交付文档、忽略规则和本地推送锁，不改变已测试的 Python 内核。

## 已核对

- “升级包 (1)”与上一包字节相同，56 项包内校验通过；已有基线为 `5d85dbd`，未重复安装旧 payload。
- 30 项新增回归覆盖排版边界、符号/引文绑定、严格引用、字段定位、重复定义与歧义、合同/科学否决区分、失败记录保留，以及显式测试镜像的冻结回放。
- 本机基础设施测试：963 passed，0 failures/errors/skips；三个数值测试文件仅在 Docker 全仓执行。
- Docker 全仓测试：1054 passed，0 failures/errors/skips，33.042 秒。两次数值验证每次 98 行、9 项诊断，两份输出逐字一致；协议摘要 `cf64bb35768545b75d00ae9c2abc8c967f691210efaef3d5c4b65356efc4b718`。使用 2 个种子（从 201 开始）；没有运行外部候选或晋级算法。
- 三模式固定回复联跑及回放全部通过。idea/scratch/revise 分别产生 83/70/5 次固定模型回复；含故障注入席位的预约计数为 85/72/7。idea 和 scratch 各完成 22 个 Docker 作业。回放均为零新增调用，产物身份一致。
- 支撑包复现、Docker 隔离负控、实际 TeX 修改稿、PaperKit 原生及参考构建通过；参考测试 86 passed。缺图仍按预期失败，未绕过失败门禁。TeX 修改稿页面已目视检查，正文、公式、表格和脚注可见。
- 实际 DOCX 修改稿在新文档镜像中渲染通过，修改前后各一页，19 个其他 ZIP 成员逐字节保留，回放无新增修改。两页均已目视检查，公式、表格、图片、编号和脚注保持可见。三模式中的 Markdown 编辑样例仍为 `DRAFT_PENDING_LAYOUT`，不会由单独的 DOCX 测试代替其版式审查。
- 新运行镜像通过正式 Dockerfile 构建，91 个应用、技能与配置文件的哈希与当前检出逐项相同。
- 对旧运行的只读检查：此前接受的 57 条事实全部通过；第 11 批最后一稿通过新结构规则。旧来源和产物未改写，此结果不是新的科学审查。

## GitHub 验收

本地工程验收完成后，`fb66eac5bd0e2146813b78cb372d24f453779386` 已非强制推送至 `origin/main`。两条工作流均在该精确提交上通过：

- [deterministic-contract-tests](https://github.com/mythrise/math_harness/actions/runs/34452456909)：基础设施、正式 Docker 构建、PaperKit、旧资料流程与 R2 回放、支撑包复现、论文返工、隔离负控和全仓数值验证。
- [three-input-modes](https://github.com/mythrise/math_harness/actions/runs/34452456848)：469 项专项测试全部通过，三模式流程与回放、支撑包、实际 DOCX/TeX 渲染通过。

远端回执已下载至本机 `reports/declaration-fix/github-{deterministic,three-inputs}/`。这些 CI 使用固定模型和检索回复，不能代替下述真实 CLI 运行。

## 真实历史题运行：操作员主动停止并清理

操作员要求停止，为正式比赛做准备。控制器退出码 130；控制器和当前 Claude 子进程均确认退出。停止前保存了 37 次真实模型回复，7 批、45 条事实通过双席审查；第 8 批尚未完成。没有执行真实 Exa 检索或数值作业，没有生成该题的最终论文。状态为 `STOPPED_BY_USER`，不是完整真实题验收通过。

本轮初始化时，原件及附件 8 个文件复制后哈希一致。原本地建议的网页来源与人工核验继续标为未证实。配置上限为 240 次模型调用、80 次 Exa HTTP 尝试，Claude 无本地截止时间。停止后按操作员明确要求删除三轮专用测试目录、旧定日镜场工作区、输入与专用原始报告，共 22 个顶层路径；因此原始题目测试材料不再随本机交付保留。仅保留不含题目正文的工程测试及停止/清理摘要，未篡改任何审查结论以伪造通过。

实现与使用说明见 [声明字段修复](../docs/BRIEF_DECLARATIONS_FIX_CN.md)。工程、固定回复、真实 CLI、真实 Exa、实际数值、实际 PDF 和人工签核分别记录，不把结构检查或固定回复结果宣传为真实赛题求解成功。
