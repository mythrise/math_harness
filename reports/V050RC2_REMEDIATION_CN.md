# v0.5 rc2 整改与验收

代码基线：`c5d807b556af217432d4f9d63b92e80784c52759`。本次受测代码：`fefa1ba1d0acda7c77cc6fba8d3b8066c59957bc`。在独立工作树修复，原目录未提交工作及旧冻结记录保留。

## 已完成工程验收

| 验收 | 新结果 | 范围 |
| --- | --- | --- |
| 完整仓库 pytest / Docker | 877 通过，0 失败 | 包括数值测试；无真实模型回复 |
| 宿主基础设施 pytest | 786 通过，0 失败 | 三组数值测试按隔离要求仅在 Docker 运行 |
| 三模式及整改专项 CI | 298 通过 | 与上两行有重叠，不能相加 |
| PaperKit 参考测试 | 86 通过 | 真实隔离构建，非真实竞赛论文质量认证 |
| 数值重复 | 两次各 98 行、9 项诊断一致 | 无外部候选执行、无全局晋级 |
| idea / scratch | 各 22 个 Docker 作业完成，隔离 TeX 生成论文产物 | 固定模型/检索回复，SYNTHETIC_FIXTURE |
| 三路重放 | 零新增调用，作业及产物摘要不变 | 包括 revise 编辑副本 |
| 实际 DOCX / TeX 改稿 | 原稿与实际改稿隔离渲染通过，已检查前后页面 | 数值/公式/引用/分页保持；原科学结论未重新验证 |
| 源码与镜像 | 三个新镜像均匹配 67 个 Python 源文件 | 本地验证镜像复用已验收依赖层，CI 另做干净构建 |
| 原始附件算法 | 408 个受保护文件摘要核验通过 | 没有修改原算法或用夹具取代真实问题 |

上述数量分别对应实际日志，各集合有重叠。早期 `9bb61e1` 的 876/785 成绩保留为中间验证，不替代最终代码的 877/786。

两条 CI 均在上述新受测 SHA 成功：

- [deterministic-contract-tests](https://github.com/mythrise/math_harness/actions/runs/34374402426)
- [three-input-modes](https://github.com/mythrise/math_harness/actions/runs/34374402369)

已下载 CI 产物，核对测试计数、隔离/支撑包/PaperKit/数值总结，以及 DOCX/TeX 渲染验收结果。

## 整改与阶段矩阵

| 问题 | 修复 | 验收依据 |
| --- | --- | --- |
| 早期材料使用最终计划审查范围 | brief、data-policy、portfolio、idea-fidelity、idea-alignment、editorial、editorial-layout 独立阶段；可信提示与技能指纹绑定 | 完整模块回归；真实新 brief 审查已针对当前锚点/遗漏条件提出否决，没有要求提前完成未来实验 |
| 同段多条建议可能遗漏 | 逐分句覆盖账本与精确原文位置；提取/合并/排除/待读均有理由 | 漏项、假锚点、第一条覆盖后续约束等负例；独立语义检查仍必需 |
| 独立基线可被无痕替换 | 规范基线身份与摘要绑定计划、实际源码分支、冻结协议及实验回执；替换需适用性说明和独立审查 | 正常保留、合法显式更换、弱化/假摘要/缺源码/错误 variant 负例 |
| 未决问题被当作解决 | 严重性、影响阶段与解决动作；关键事项到阶段前阻断，非关键明确暂缓 | 关键假关闭/暂缓负例、非关键暂缓正例 |
| 数字集合不变仍能篡改结论 | 保留实体、指标、值、单位/方向的科学分句关联，并保留独立审查 | 多实体、百分比、比较、上下限、否定与因果交换负例；合法语言调整/整句重排正例 |
| 修订副本缺实际版式证据 | 实际修改稿先隔离渲染，再做摘要绑定的逐页审查；缺工具仅输出 DRAFT_PENDING_LAYOUT | 实际 DOCX/TeX 前后渲染；生产编排、缺工具、未知进程、篡改与零调用重放回归 |
| 无效配置可进入外部调用 | 计数/FE/线程/种子严格整数，时长有限正数；合法零退避保留；新旧入口前置校验 | bool、1.5、NaN/Inf 等负例验证外部调用计数为零 |
| brief 返修缺具体上下文 | 报告具体 Q-ID、非法引用和实际类型；提供最新实际产物，明确 given/constraint 映射规则 | 真实重跑中复现后增加回归，不改旧失败或降低校验 |

详细合同与边界见 [整改说明](../docs/V050RC2_REMEDIATION_CN.md)。所有固定回复测试都不能证明真实题已完成。

## 真实定日镜场

**最终状态：BLOCKED / ScientificRejection；0 个真实实验作业，没有真实结果论文，继续保持 RC。**

新真实测试集中于 `/Users/mythrise/数学建模国赛/定日镜场真实测试/20260909-v050rc2/`，含官方输入副本、原本地研究要求、来源摘要、各个新工作区、日志、运行费用及逐分句去向报告。旧冻结工作区留在原位。共享 Exa 费用账本和凭证不属于可清除测试文件。

第一轮新工作区 `workspace-idea-01` 已终止：4 次 Codex 调用、4 次真实 Exa 请求，brief 的约束引用类型错误被拒绝，随后返修调用在 300 秒超时。0 个实验作业，未完成 Claude 审查。没有未知在途；负记录保留。

补清映射提示和返修定位信息后，另建 `workspace-idea-02`。剩余授权为最多 96 次模型调用、28 次 HTTP 请求，单次生成等待上限 600 秒；原审查规则不变。首份 brief 已通过结构检查，真实独立审查进一步发现标题锚点不支撑具体字段、公式锚点残缺及当前给定条件漏提。一次有界返修后，两席仍否决：部分已知定义缺失、内部条目引用悬空，且把原题已经规定的时间口径重新列成歧义。该缺失属于当前题意阶段，原样保留有效否决；没有继续刷票。最终 brief 恰好达到 schema 的 120 条上限，这是观察到的容量饱和，不能据此认定上限是全部遗漏的唯一原因。

旧 c5d807b 原始失败复查区分了两类原因：第一次约束引用类型错误是有效当前阶段缺陷；修正后的 brief 被按 plan_design 要求完整推导、decoder 和冻结实验规格，属于已证明的阶段错配。物理模型、可行域、算法适用性和实验公平性仍是正式建模必须解决的问题。

两轮累计预留 14 次真实 CLI 调用（Codex 12 次，其中 1 次超时；Claude 2 次，均在 180 秒超时），真实 Exa 请求 8 次，已知 Exa 费用合计 **0.056 USD**。CLI 费用未返回，模型总费用为 UNKNOWN，不记为免费。可读取回执报告输入 360335 tokens、输出 39923 tokens；超时工作可能未包含。总授权上限仍为 100 次模型 / 32 次 HTTP。两个工作区均无 RUNNING 步骤/作业，8 次 Exa 均已响应，共享活动租约为 0；输入与事件链审计通过。

本机材料中的 69 个分句有只读生成的来源去向报告。只有控制器实际接受的提取、处置、计划、代码、自测、作业和论文才填入对应证据；未到达阶段记 NOT_REACHED，不能把分句位置或计划当成实测。

## NOT_RUN 与限制

- 未提供有来源记录的真实网页初稿，因此“真实网页会话初版 → 全链路论文”的验收为 NOT_RUN；本地研究要求未冒充网页聊天。
- 未收到独立函数级探针附件。回归根据整改文件描述在完整模块上重现问题，包含正反例。
- 真实整题实验、最终论文与科学结论验证未完成；本轮已终止于题意审查，不存在可交付的真实结果论文。
- 没有代签、自动提交比赛、伪造科学结果或变更冻结评测器/确认集。获奖结果不作保证。

## 交付索引

- [源码 diff 与验收证据目录](v050rc2-acceptance/README.md)：含完整 patch、新 SHA 测试计数及原日志摘要、镜像一致性、三模式重放和 CI 回执。
- [实际修改稿版式报告](v050rc2-acceptance/actual-document-layout-summary.json)：原稿/改稿 PNG 与摘要，限定于这两对实际测试文档。
- [真实运行与费用摘要](v050rc2-acceptance/live-summary.json)：两轮终止原因、原始回执摘要、真实服务调用与费用边界。
- 本地完整逐条报告：`定日镜场真实测试/20260909-v050rc2/reports/idea-trajectory-02.md`，所有 69 条保留原文位置和真实阶段状态；原题、附件、原文与完整模型审查不推到公开仓库。

可运行修订位于 `.runtime/v050-remediation-worktree/`，使用该目录的 `./scripts/cumcm` 或 `.venv/bin/python`。主目录原有未提交修改与源文件删除保持原状，没有覆盖用户工作。报告提交仅补齐证据与文字；新 CI 对应的运行代码为上文明确记录的 fefa1ba。
