# LaTeX template source

The executable 2026 profile is `cumcm_harness/paper.py:PREAMBLE` and `build_paper`.
The fully generated, compiled example is `examples/validated_run/paper/main.tex`.
This is a self-developed profile based on official rules, not an official CUMCM class.
All custom source and required runtime assets are packaged; no font files are distributed.
